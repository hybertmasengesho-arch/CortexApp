<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Cortex">

        <!-- Simple WebView shell that opens your live Cortex site. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          The actual Quick Settings tile. android:icon must point at a flat,
          single-color drawable (see res/drawable/ic_tile_share.png) — the
          system tints it automatically based on the tile's active/inactive
          state, so it should NOT be the colorful launcher icon.
        -->
        <service
            android:name=".ShareTileService"
            android:label="@string/tile_label"
            android:icon="@drawable/ic_tile_share"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

    </application>
</manifest>
